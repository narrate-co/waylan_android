package space.narrate.waylan.core.ui

import space.narrate.waylan.core.R

enum class ListType(val titleRes: Int) {
    TRENDING(R .string.title_trending),
    RECENT(R.string.title_recent),
    FAVORITE(R.string.title_favorite)
}