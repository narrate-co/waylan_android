package space.narrate.words.android.data.firestore

/**
 * An enum to hold the value of where data originated
 *
 * If user's are allowed to modify wordset db data in the future, this is
 * to flag those edits ad user edits
 */
enum class DataOwners {
    WORDSET
}

